# Rust wrapper for OP-TEE Client (libteec) library

`optee-teec` allows Client Application developers to write their applications in
Rust in order to communicate with their conterpart Trusted Applications (TAs).
